import {all, fork} from 'redux-saga/effects';

// import auth from './auth';
// import story from './story';
// import profile from './profile';

export default function* home() {
  //   yield all([fork(story), fork(auth), fork(profile)]);
}
